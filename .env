# .env file inside the server directory

# Port for the signaling server to listen on (Render sets this automatically, but you can override it here if needed)
SIGNALING_PORT=4000

# CORS configuration for the signaling server (only allow requests from your Vercel domain)
CORS_ORIGIN=https://swisscom-ai.vercel.app  # Change this to your Vercel app URL
